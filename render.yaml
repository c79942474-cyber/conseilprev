services:
  - type: web
    name: conseilprev
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app --worker-class gthread --threads 8 --timeout 90 --keep-alive 5
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
      - key: DATABASE_URL
        fromDatabase:
          name: conseilprev-registre-db
          property: connectionString

databases:
  - name: conseilprev-registre-db
    plan: free
    databaseName: registre_ia
    user: conseilprev
